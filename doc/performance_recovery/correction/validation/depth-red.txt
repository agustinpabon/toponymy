.FFFFF                                                                   [100%]
=================================== FAILURES ===================================
__________ test_standalone_tree_releases_consumed_grouping_buffers[3] __________

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x110c0c650>, depth = 3

    @pytest.mark.parametrize("depth", [1, 3, 8, 16, 32, 64])
    def test_standalone_tree_releases_consumed_grouping_buffers(monkeypatch, depth):
        """Grouping storage stays bounded in depth; no timing/allocation threshold."""
        original = clustering._group_labels
        buffers = []

        def track_grouping(labels):
            # The consuming loop may still hold the previous group while computing
            # the next one. Earlier groups must no longer be retained.
            assert sum(reference() is not None for reference in buffers) <= 1
            group = original(labels)
            buffers.append(weakref.ref(group[2]))
            return group

        monkeypatch.setattr(clustering, "_group_labels", track_grouping)
        labels = [np.array([19, -1, np.iinfo(np.int64).max, 19])] * depth
>       tree = clustering.build_cluster_tree(labels)
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/test_performance_clustering.py:96:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
toponymy/clustering.py:84: in build_cluster_tree
    return _tree_from_grouping(labels, [_group_labels(layer) for layer in labels])
                                        ^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

labels = array([                 19,                  -1, 9223372036854775807,
                        19])

    def track_grouping(labels):
        # The consuming loop may still hold the previous group while computing
        # the next one. Earlier groups must no longer be retained.
>       assert sum(reference() is not None for reference in buffers) <= 1
E       assert 2 <= 1
E        +  where 2 = sum(<generator object test_standalone_tree_releases_consumed_grouping_buffers.<locals>.track_grouping.<locals>.<genexpr> at 0x1076b6cf0>)

tests/test_performance_clustering.py:89: AssertionError
__________ test_standalone_tree_releases_consumed_grouping_buffers[8] __________

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x110c0dc10>, depth = 8

    @pytest.mark.parametrize("depth", [1, 3, 8, 16, 32, 64])
    def test_standalone_tree_releases_consumed_grouping_buffers(monkeypatch, depth):
        """Grouping storage stays bounded in depth; no timing/allocation threshold."""
        original = clustering._group_labels
        buffers = []

        def track_grouping(labels):
            # The consuming loop may still hold the previous group while computing
            # the next one. Earlier groups must no longer be retained.
            assert sum(reference() is not None for reference in buffers) <= 1
            group = original(labels)
            buffers.append(weakref.ref(group[2]))
            return group

        monkeypatch.setattr(clustering, "_group_labels", track_grouping)
        labels = [np.array([19, -1, np.iinfo(np.int64).max, 19])] * depth
>       tree = clustering.build_cluster_tree(labels)
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/test_performance_clustering.py:96:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
toponymy/clustering.py:84: in build_cluster_tree
    return _tree_from_grouping(labels, [_group_labels(layer) for layer in labels])
                                        ^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

labels = array([                 19,                  -1, 9223372036854775807,
                        19])

    def track_grouping(labels):
        # The consuming loop may still hold the previous group while computing
        # the next one. Earlier groups must no longer be retained.
>       assert sum(reference() is not None for reference in buffers) <= 1
E       assert 2 <= 1
E        +  where 2 = sum(<generator object test_standalone_tree_releases_consumed_grouping_buffers.<locals>.track_grouping.<locals>.<genexpr> at 0x110c24e10>)

tests/test_performance_clustering.py:89: AssertionError
_________ test_standalone_tree_releases_consumed_grouping_buffers[16] __________

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x110c0e8a0>
depth = 16

    @pytest.mark.parametrize("depth", [1, 3, 8, 16, 32, 64])
    def test_standalone_tree_releases_consumed_grouping_buffers(monkeypatch, depth):
        """Grouping storage stays bounded in depth; no timing/allocation threshold."""
        original = clustering._group_labels
        buffers = []

        def track_grouping(labels):
            # The consuming loop may still hold the previous group while computing
            # the next one. Earlier groups must no longer be retained.
            assert sum(reference() is not None for reference in buffers) <= 1
            group = original(labels)
            buffers.append(weakref.ref(group[2]))
            return group

        monkeypatch.setattr(clustering, "_group_labels", track_grouping)
        labels = [np.array([19, -1, np.iinfo(np.int64).max, 19])] * depth
>       tree = clustering.build_cluster_tree(labels)
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/test_performance_clustering.py:96:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
toponymy/clustering.py:84: in build_cluster_tree
    return _tree_from_grouping(labels, [_group_labels(layer) for layer in labels])
                                        ^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

labels = array([                 19,                  -1, 9223372036854775807,
                        19])

    def track_grouping(labels):
        # The consuming loop may still hold the previous group while computing
        # the next one. Earlier groups must no longer be retained.
>       assert sum(reference() is not None for reference in buffers) <= 1
E       assert 2 <= 1
E        +  where 2 = sum(<generator object test_standalone_tree_releases_consumed_grouping_buffers.<locals>.track_grouping.<locals>.<genexpr> at 0x110c26740>)

tests/test_performance_clustering.py:89: AssertionError
_________ test_standalone_tree_releases_consumed_grouping_buffers[32] __________

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x110c0f350>
depth = 32

    @pytest.mark.parametrize("depth", [1, 3, 8, 16, 32, 64])
    def test_standalone_tree_releases_consumed_grouping_buffers(monkeypatch, depth):
        """Grouping storage stays bounded in depth; no timing/allocation threshold."""
        original = clustering._group_labels
        buffers = []

        def track_grouping(labels):
            # The consuming loop may still hold the previous group while computing
            # the next one. Earlier groups must no longer be retained.
            assert sum(reference() is not None for reference in buffers) <= 1
            group = original(labels)
            buffers.append(weakref.ref(group[2]))
            return group

        monkeypatch.setattr(clustering, "_group_labels", track_grouping)
        labels = [np.array([19, -1, np.iinfo(np.int64).max, 19])] * depth
>       tree = clustering.build_cluster_tree(labels)
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/test_performance_clustering.py:96:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
toponymy/clustering.py:84: in build_cluster_tree
    return _tree_from_grouping(labels, [_group_labels(layer) for layer in labels])
                                        ^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

labels = array([                 19,                  -1, 9223372036854775807,
                        19])

    def track_grouping(labels):
        # The consuming loop may still hold the previous group while computing
        # the next one. Earlier groups must no longer be retained.
>       assert sum(reference() is not None for reference in buffers) <= 1
E       assert 2 <= 1
E        +  where 2 = sum(<generator object test_standalone_tree_releases_consumed_grouping_buffers.<locals>.track_grouping.<locals>.<genexpr> at 0x110c269b0>)

tests/test_performance_clustering.py:89: AssertionError
_________ test_standalone_tree_releases_consumed_grouping_buffers[64] __________

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x110c0f500>
depth = 64

    @pytest.mark.parametrize("depth", [1, 3, 8, 16, 32, 64])
    def test_standalone_tree_releases_consumed_grouping_buffers(monkeypatch, depth):
        """Grouping storage stays bounded in depth; no timing/allocation threshold."""
        original = clustering._group_labels
        buffers = []

        def track_grouping(labels):
            # The consuming loop may still hold the previous group while computing
            # the next one. Earlier groups must no longer be retained.
            assert sum(reference() is not None for reference in buffers) <= 1
            group = original(labels)
            buffers.append(weakref.ref(group[2]))
            return group

        monkeypatch.setattr(clustering, "_group_labels", track_grouping)
        labels = [np.array([19, -1, np.iinfo(np.int64).max, 19])] * depth
>       tree = clustering.build_cluster_tree(labels)
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/test_performance_clustering.py:96:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
toponymy/clustering.py:84: in build_cluster_tree
    return _tree_from_grouping(labels, [_group_labels(layer) for layer in labels])
                                        ^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

labels = array([                 19,                  -1, 9223372036854775807,
                        19])

    def track_grouping(labels):
        # The consuming loop may still hold the previous group while computing
        # the next one. Earlier groups must no longer be retained.
>       assert sum(reference() is not None for reference in buffers) <= 1
E       assert 2 <= 1
E        +  where 2 = sum(<generator object test_standalone_tree_releases_consumed_grouping_buffers.<locals>.track_grouping.<locals>.<genexpr> at 0x110e14930>)

tests/test_performance_clustering.py:89: AssertionError
=========================== short test summary info ============================
FAILED tests/test_performance_clustering.py::test_standalone_tree_releases_consumed_grouping_buffers[3]
FAILED tests/test_performance_clustering.py::test_standalone_tree_releases_consumed_grouping_buffers[8]
FAILED tests/test_performance_clustering.py::test_standalone_tree_releases_consumed_grouping_buffers[16]
FAILED tests/test_performance_clustering.py::test_standalone_tree_releases_consumed_grouping_buffers[32]
FAILED tests/test_performance_clustering.py::test_standalone_tree_releases_consumed_grouping_buffers[64]
5 failed, 1 passed, 22 deselected in 1.99s
